<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomScrollViewStyle">
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_thumb</item>
        <item name="android:scrollbarTrackVertical">@drawable/scrollbar_track</item>
        <item name="android:fadeScrollbars">false</item> <!-- Itt implementálva ezért a kódban nem kell a **isHorizontalScrollBarEnabled = false** sor
         || Here implemented so that the code does not need the **isHorizontalScrollBarEnabled = false** line -->
        <item name="android:scrollbars">vertical</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
    </style>
</resources>